import React, { FunctionComponent } from 'react';

const NotificationsView: FunctionComponent<any> = (props: any) => {
    return (
        <div>Notifications</div>
    );
}

export default NotificationsView;