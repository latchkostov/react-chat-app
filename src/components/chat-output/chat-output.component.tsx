import React, { FunctionComponent } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
`;

const ChatOutput: FunctionComponent<any> = (props: any) => {
    return (
        <Wrapper>
            Chat output
        </Wrapper>
    );
}

export default ChatOutput;